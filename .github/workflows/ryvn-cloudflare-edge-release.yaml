name: Build & Release cloudflare-edge

on:
  workflow_dispatch:
  push:
    branches:
      - "main"
    paths:
      - "cloudflare/cloudflare-edge/**"
      - ".github/workflows/ryvn-cloudflare-edge-release.yaml"
  pull_request:
    paths:
      - "cloudflare/cloudflare-edge/**"
      - ".github/workflows/ryvn-cloudflare-edge-release.yaml"

jobs:
  release:
    uses: ryvn-technologies/ryvn-build-action/.github/workflows/release.yml@v2
    with:
      service_name: cloudflare-edge
    permissions:
      contents: write
      id-token: write
    secrets:
      RYVN_CLIENT_ID: ${{ secrets.RYVN_CLIENT_ID }}
      RYVN_CLIENT_SECRET: ${{ secrets.RYVN_CLIENT_SECRET }}
