name: Build & Release aws-workload-identity

on:
  workflow_dispatch:
  push:
    branches:
      - "main"
    paths:
      - "aws/workload-identity/**"
      - ".github/workflows/ryvn-aws-workload-identity-release.yaml"
  pull_request:
    paths:
      - "aws/workload-identity/**"
      - ".github/workflows/ryvn-aws-workload-identity-release.yaml"

jobs:
  release:
    uses: ryvn-technologies/ryvn-build-action/.github/workflows/release.yml@v2
    with:
      service_name: aws-workload-identity
      tag_prefix: aws-workload-identity@
    permissions:
      contents: write
      id-token: write
