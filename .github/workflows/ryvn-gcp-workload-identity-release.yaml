name: Build & Release gcp-workload-identity

on:
  workflow_dispatch:
  push:
    branches:
      - "main"
    paths:
      - "gcp/workload-identity/**"
      - ".github/workflows/ryvn-gcp-workload-identity-release.yaml"
  pull_request:
    paths:
      - "gcp/workload-identity/**"
      - ".github/workflows/ryvn-gcp-workload-identity-release.yaml"

jobs:
  release:
    uses: ryvn-technologies/ryvn-build-action/.github/workflows/release.yml@v2
    with:
      service_name: gcp-workload-identity
      tag_prefix: gcp-workload-identity@
    permissions:
      contents: write
      id-token: write
