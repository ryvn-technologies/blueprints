name: Build & Release gcp-workload-access

on:
  workflow_dispatch:
  push:
    branches:
      - "main"
    paths:
      - "gcp/workload-access/**"
      - ".github/workflows/ryvn-gcp-workload-access-release.yaml"
  pull_request:
    paths:
      - "gcp/workload-access/**"
      - ".github/workflows/ryvn-gcp-workload-access-release.yaml"

jobs:
  release:
    uses: ryvn-technologies/ryvn-build-action/.github/workflows/release.yml@v2
    with:
      service_name: gcp-workload-access
    permissions:
      contents: write
      id-token: write
