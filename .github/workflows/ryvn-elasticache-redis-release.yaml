name: Build & Release elasticache-redis

on:
  workflow_dispatch:
  push:
    branches:
      - "main"
    paths:
      - "aws/elasticache/**"
      - ".github/workflows/ryvn-elasticache-redis-release.yaml"
  pull_request:
    branches:
      - "main"
    paths:
      - "aws/elasticache/**"

jobs:
  release:
    uses: ryvn-technologies/ryvn-build-action/.github/workflows/release.yml@v2
    with:
      service_name: elasticache-redis
    permissions:
      contents: write
      id-token: write
    secrets:
      RYVN_CLIENT_ID: ${{ secrets.RYVN_CLIENT_ID }}
      RYVN_CLIENT_SECRET: ${{ secrets.RYVN_CLIENT_SECRET }}
