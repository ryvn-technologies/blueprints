# Private Service Consumer (GCP)

Connects this VPC to a Private Service Publisher: a Private Service Connect endpoint that only accepts TCP 80 and 443, and a private DNS zone that points `*.<publisher_domain>` at it.
