# Private Service Consumer (AWS)

Connects this VPC to a Private Service Publisher: an interface endpoint that only accepts TCP 80 and 443, and a private hosted zone that points `*.<publisher_domain>` at it.
