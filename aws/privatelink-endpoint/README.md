# PrivateLink Endpoint (AWS)

Connects this VPC to a PrivateLink endpoint service: an interface endpoint that only accepts TCP 80 and 443, and a private hosted zone that points `*.<private_hosted_zone_name>` at it. Backs the AWS side of Private Service Consumer.
